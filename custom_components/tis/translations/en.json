{
  "config": {
    "step": {
      "user": {
        "title": "TIS Smart Home System",
        "description": "Enter TIS gateway IP address and UDP port",
        "data": {
          "gateway_ip": "Gateway IP Address",
          "udp_port": "UDP Port"
        }
      }
    },
    "abort": {
      "already_configured": "TIS system is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "TIS Settings",
        "description": "Change gateway IP and port settings",
        "data": {
          "gateway_ip": "Gateway IP Address",
          "udp_port": "UDP Port"
        }
      }
    }
  }
}
